# `src/catalog/store.rs`

Owns the shared in-memory catalog store, lookup/list operations, registration,
rename, removal, rollback-aware mutation behavior, and unit tests for the core
store invariants.
