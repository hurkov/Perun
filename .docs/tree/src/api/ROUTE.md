# API Route

This route describes only the contents of the mirrored `src/api/` folder.

| Entry | Description |
|---|---|
| `error.md` | Description of the real `src/api/error.rs`. |
| `mod.md` | Description of the real `src/api/mod.rs`. |
| `sounds/` | Sound-management HTTP feature; see `sounds/ROUTE.md`. |
