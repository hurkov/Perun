# `src/api/mod.rs`

Composes the health route, exposes the shared API error module, nests the configured sounds router under `/api/sounds`, and forwards the playback sender into the sound feature state.
