# `src/api/sounds/mod.rs`

Registers sound routes, builds sound feature state from the catalog store, runtime settings, and playback sender, and applies the configured upload body limit to `/upload`.
