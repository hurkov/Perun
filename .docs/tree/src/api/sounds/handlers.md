# `src/api/sounds/handlers.rs`

Validates HTTP input, calls catalog operations, enqueues playback commands for `/play`, maps results and `ApiError` into HTTP responses, and keeps feature-local handler tests beside the implementation.
