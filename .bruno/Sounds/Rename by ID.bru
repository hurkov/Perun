meta {
  name: Rename by ID
  type: http
  seq: 7
}

patch {
  url: {{baseUrl}}/api/sounds/rename
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

params:query {
  id: {{soundId}}
}

body:json {
  {
    "title": "{{newSoundTitle}}"
  }
}
