meta {
  name: Rename by Title
  type: http
  seq: 8
}

patch {
  url: {{baseUrl}}/api/sounds/rename
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

params:query {
  title: {{soundTitle}}
}

body:json {
  {
    "title": "{{newSoundTitle}}"
  }
}
